# Alternative workflow that auto-detects preview URL
name: RUM Crawl (Auto-detect Preview)
on:
  workflow_dispatch:
  deployment_status:

jobs:
  crawl:
    runs-on: ubuntu-latest
    if: github.event.deployment_status.state == 'success' && github.event.deployment_status.environment_url != ''
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npx playwright install --with-deps

      - name: Generate routes.json (placeholder)
        run: echo "Creating placeholder routes.json" && echo '["/" , "/dashboard"]' > routes.json

      - name: RUM crawl placeholder
        run: echo "RUM crawl scripts not yet implemented - skipping for now"